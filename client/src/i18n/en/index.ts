import { LocaleMessages } from '@/i18n/type';

import { common } from './common';
import { layouts } from './layouts';
import { pages } from './pages';

export const en: { translation: LocaleMessages } = {
  translation: {
    common,
    layouts,
    pages,
  },
};
